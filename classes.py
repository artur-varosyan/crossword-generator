class Word:
    def __init__(self, name, orientation, starting_row, starting_column, length):
        self.name = name
        self.orientation = orientation
        self.starting_row = starting_row
        self.starting_column = starting_column
        self.length = length
